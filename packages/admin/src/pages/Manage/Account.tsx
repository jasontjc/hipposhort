import React, { type FC } from 'react'
import { observer } from 'mobx-react-lite'
import styles from './account.modulel.less'

const Account: FC = () => {
  return <div>Account</div>
}

export default observer(Account)
