import React, { type FC } from 'react'
import { observer } from 'mobx-react-lite'
import styles from './role.modulel.less'

const Role: FC = () => {
  return <div>Role</div>
}

export default observer(Role)
