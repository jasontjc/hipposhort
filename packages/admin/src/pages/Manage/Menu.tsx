import React, { type FC } from 'react'
import { observer } from 'mobx-react-lite'
import styles from './menu.modulel.less'

const Menu: FC = () => {
  return <div>Menu</div>
}

export default observer(Menu)
